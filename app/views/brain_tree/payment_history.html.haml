#wrapper
  
  %section

    .row
      .small-12.columns.animated{data: {animate: "fadeInDown", animate: "0"}}
        .title-section
          %h3 Payment History
          -# %p NOTE: For users who have unsubscribed from Tengence before, the table below will not reflect your previous subscription(s).
    
    .row

      .small-12.column
        %table{role: "grid", width: '100%'}

          %thead

            %tr
              %th Time of Settlement
              %th Amount (USD)
              %th Credit Card
              %th Billing Period

          %tbody
            - @subscription.transactions.each do |transaction|
              %tr
                %td= "#{@subscription.created_at.strftime('%d %b %Y %H:%M %Z')}"
                %td= transaction.amount
                %td
                  %img{src: transaction.credit_card_details.image_url}
                  %strong=transaction.credit_card_details.card_type
                  ending with #{transaction.credit_card_details.last_4}
                %td= billing_period(transaction.subscription_details)