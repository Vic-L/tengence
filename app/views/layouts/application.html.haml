!!!
%html.no-js{:lang => "en"}
  %head
    %meta{:charset => "utf-8"}
    %meta{:content => "width=device-width, initial-scale=1.0", :name => "viewport"}
    = metamagic title: "Tengence", description: "We Track Opportunities from >60 Sources in Singapore . Get Daily Email Notifications. Keyword Tracking. Sign Up Now!"
    %link{:href => "https://fonts.googleapis.com/css?family=Montserrat:400,700", :rel => "stylesheet", :type => "text/css"}
    %link{:href => "https://fonts.googleapis.com/css?family=Roboto:400,700|Roboto+Condensed:700,400", :rel => "stylesheet", :type => "text/css"}
    = stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true
    = csrf_meta_tags
    %script{:src => "https://www.google.com/recaptcha/api.js"}
  
  %body{class: "#{params[:controller].parameterize} #{params[:action].parameterize}"}
    
    :javascript
      dataLayer = [];
      var GA_tender_id = null, GA_UID = null,
        GA_access_level = null;
    
    = render 'layouts/header'
    = yield
  
    = javascript_include_tag "application", "data-turbolinks-track" => true
    / End Style Switcher
    :javascript
      $(document).foundation();
      $(document).on('opened.fndtn.reveal', '[data-reveal]', function () {
        document.body.classList.remove('loading');
      });
      /* trigger foundation() after window load for react and foundation to work...TODO better way? */
      $(window).load(function(){
        $(document).foundation();
      });
    = yield :javascript
    = render 'layouts/ga_scripts'